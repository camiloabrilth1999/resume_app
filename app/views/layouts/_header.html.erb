<nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom">
  <%- if user_signed_in? %>
  <button class="btn btn-primary" id="menu-toggle"><i class="fa fa-align-justify" aria-hidden="true"></i></button>
  <% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto mt-2 mt-lg-0">
      <li class="nav-item nav-item-custom active">
        
      </li>
      <%- if !user_signed_in? %>
        <li class="nav-item nav-item-custom">
          <a class="nav-link" href="<%=new_user_session_path%>">Inicia sesión</a>
        </li>
        <li class="nav-item nav-item-custom">
          <a class="nav-link" href="<%=new_user_registration_path%>">Registrate</a>
        </li>
      <% else %>
        <li class="nav-item nav-item-custom"> <%= link_to current_user.name,   user_path(current_user.id), class: "nav-link" %></li>
        <li class="nav-item nav-item-custom"> <%= link_to "Salir",destroy_user_session_path, method: :delete, class: "nav-link" %></li>
      <% end %>
    </ul>
  </div>
</nav>
